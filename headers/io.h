// Inserting data in the library structure
int fetch_data(char* path, int k);

// Performs query and prints out the results
int ask_query(void * data);

// Release all memory for exiting
int destroy_data(void);